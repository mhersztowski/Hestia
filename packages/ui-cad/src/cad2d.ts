/**
 * `@hestia/ui-cad/cad2d` — the 2D CAD page and its drawing engine.
 *
 * A file of its own next to `index.ts` so that the areas of this package can be
 * imported apart from one another: what the tree looks like inside is
 * `src/cad2d/`, and this is the door to it.
 */
export * from './cad2d/barrel';
